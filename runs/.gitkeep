# Keeps runs/ tracked in git. Per-run structured JSON dumps land here from
# Phase 6 onward (spec §5.6, §8) and are committed for read-by-eye auditing.
# Prune to ~90 days (spec §13); do NOT gitignore this directory.
