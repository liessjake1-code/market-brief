{# Email-safe brief template (Section 5, 10.4). Built in Phase 7.

   Constraints when implemented:
   - Single-column TABLE layout, fully inline styles, web-safe fonts (clients
     strip <style>, ignore flex/grid, block web fonts).
   - Three fenced zones: settled recap, "This morning so far" (visually fenced,
     timestamped), What to Watch Today.
   - Tabular numerals (Consolas/SFMono monospace) for every figure.
   - Degraded banner at top when degraded; favicons only in Movers/Watchlist.
   - Render from validated fields only; the template enforces house style.

   PLACEHOLDER until Phase 7. #}
<!doctype html>
<html>
  <body>
    <p>Daily Market Brief template not yet built (Phase 7).</p>
  </body>
</html>
